#include "math_operation.h"

namespace my::math {
double Add(double first, double second) { return first + second; }
float Add(float first, float second) { return first + second; }
}  // namespace my::math
